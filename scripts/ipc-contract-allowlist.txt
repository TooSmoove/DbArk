# IPC contract allowlist (audit H-3)
#
# Commands listed here are permitted to return a bare `String` or `bool` instead
# of the canonical `Result<T, IpcError>` envelope. Adding a NEW bare String/bool
# command makes scripts/check-ipc-contract.sh fail. Removing an entry (after
# migrating it) is always fine.
#
# As of the H-3 migration the only entry is a genuine boolean *value* (not a
# success flag). The legacy String/bool batch has been fully migrated.
#
# Format: one command name per line. Blank lines and # comments ignored.

# --- genuine boolean value (NOT a success flag) — permanently allowed ---
is_tunnel_open
